<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>链表初步</title>
    <link href="/2024/11/27/%E9%93%BE%E8%A1%A8%E5%88%9D%E6%AD%A5/"/>
    <url>/2024/11/27/%E9%93%BE%E8%A1%A8%E5%88%9D%E6%AD%A5/</url>
    
    <content type="html"><![CDATA[<h1 id="链表初步"><a href="#链表初步" class="headerlink" title="链表初步"></a>链表初步</h1><p>最近也是开始入算法的坑，学校要求完成一个小的C语言项目实践，主要用到链表和文件处理，索性先对链表做一个简单的总结。</p><h2 id="认识链表"><a href="#认识链表" class="headerlink" title="认识链表"></a>认识链表</h2><p>要创建链表，我们首先要了解链表的本质：一连串的结构体或者说对象（由于是C语言，我在下文统一称为结构体），每个结构体中都保存了下个结构体的指针（当然，这并不绝对），也就是说，链表的存储并不是类似于数组的存放在一片连续的内存空间，而是存放在随机的内存空间中</p><p>现在，来创造一个简单的链表的结构</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs C"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">ListNode</span>&#123;</span><br>    <span class="hljs-type">int</span> num;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">ListNode</span> *<span class="hljs-title">next</span>;</span><br>&#125;;<span class="hljs-comment">//一般的表达方式</span><br><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">ListNode</span>&#123;</span><br>    <span class="hljs-type">int</span> num;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">ListNode</span> *<span class="hljs-title">next</span>;</span><br>&#125;Node;<span class="hljs-comment">//为了后文书写方便，我们直接定义类型Node</span><br></code></pre></td></tr></table></figure><p>再来看看创建列表的一个单元</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs c">Node n1=&#123;n1.num=<span class="hljs-number">1</span>, n1.next = <span class="hljs-literal">NULL</span>&#125;;<br>Node *n2;<br>n2-&gt;num=<span class="hljs-number">2</span>;n2-&gt;next=<span class="hljs-literal">NULL</span>;<span class="hljs-comment">//注意此处n2-&gt;num即指针n2指向的Node中的num</span><br></code></pre></td></tr></table></figure><p>由于链表中我们会涉及到大量的地址操作，我们一般使用n2对应的创建方式</p><p>让我们再来创建一个真正的连续的链表</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs c">Node *n1;<br>n1-&gt;num=<span class="hljs-number">1</span>;<br>Node *n2;<br>n2-&gt;num=<span class="hljs-number">2</span>;<br>Node *n3;<br>n3-&gt;num=<span class="hljs-number">3</span>;<br>n1-&gt;next=n2;<br>n2-&gt;next=n3;<br>n3-&gt;next=<span class="hljs-literal">NULL</span>;<br></code></pre></td></tr></table></figure><p>(注意到了吗,根据约定成俗的规矩.链表的最后一个元素的next应当为NULL)</p><p>这种创建方式当然没什么大问题,但是值得注意的是,这些链表单元是出于堆空间的,注定是要在函数生成的,也就是说,在函数执行完毕后,它们所占用的空间将会被释放,这显然和我们的目的不符,所以一般我们会进行如下的改造</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">Node *n1=<span class="hljs-built_in">malloc</span>(<span class="hljs-keyword">sizeof</span> (Node));<br></code></pre></td></tr></table></figure><p>这样,这些单元的空间将不会被自动释放</p><h2 id="链表的创建"><a href="#链表的创建" class="headerlink" title="链表的创建"></a>链表的创建</h2><p>显然,按照上面的方法创建链表要一个个写,太过于不方便,也太过于缺乏实用性,所以我们选择写两个函数来完成链表的创建</p><p>首先是创建链表单元的函数</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c">Node* <span class="hljs-title function_">creatNode</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span>&#123;<br>    Node *n1= <span class="hljs-built_in">malloc</span>(<span class="hljs-keyword">sizeof</span> (Node));<br>    n1-&gt;next=<span class="hljs-literal">NULL</span>;<br>    n1-&gt;num=x;<br>    <span class="hljs-keyword">return</span> n1;<br>&#125; <br></code></pre></td></tr></table></figure><p>很简单不做过多的赘述</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs c">Node *head=<span class="hljs-literal">NULL</span>;<br>Node *end=<span class="hljs-literal">NULL</span>;<br><span class="hljs-type">void</span> <span class="hljs-title function_">addNode</span><span class="hljs-params">(Node* n1)</span>&#123;<br>    <span class="hljs-keyword">if</span>(head==<span class="hljs-literal">NULL</span>)&#123;<br>        head=end=n1;<br>    &#125;<span class="hljs-keyword">else</span>&#123;<br>        end-&gt;next=n1;<br>        end=n1;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>这里有必要简单的解释一下,我们创建了两个Node指针来保存链表的首尾如果链表为空,则新插入的链表单元为首尾,如果链表不为空,则将尾单元的next指向新插入的单元,再将尾单元更新为新插入的单元,还是很简单</p><h2 id="链表对应单元的修改与删除"><a href="#链表对应单元的修改与删除" class="headerlink" title="链表对应单元的修改与删除"></a>链表对应单元的修改与删除</h2><p>就像数组那样,链表也应当可以做到任意的增删改查,我相信看到这里的你肯定可以想到怎样进行删除,修改,查找等基础操作,但还是容许我讲讲</p><p>就像这样</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">delete</span><span class="hljs-params">(Node *n1  )</span>&#123;<br>    Node* left=head;<br>    <span class="hljs-keyword">while</span>(left-&gt;next!=n1 &amp;&amp; left)&#123;<br>        left=left-&gt;next;<br>    &#125;<br>    left-&gt;next=left-&gt;next-&gt;next;<br>    <span class="hljs-built_in">free</span>(n1);<br>&#125;<br></code></pre></td></tr></table></figure><p>最后不要忘记释放对应的空间</p><p>然后呢再考虑修改，这个的原理和删除完全相同，我就不过多讲解了</p><h2 id="链表对应单元的查找"><a href="#链表对应单元的查找" class="headerlink" title="链表对应单元的查找"></a>链表对应单元的查找</h2><p>还是很简单，可以通过一个指针来遍历链表找出符合要求的项</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c">Node* <span class="hljs-title function_">select</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span>&#123;<br>    Node* left=head;<br>    <span class="hljs-keyword">while</span>(left-&gt;num!=x &amp;&amp; left !=<span class="hljs-literal">NULL</span>)&#123;<br>        left=left-&gt;next;<br>    &#125;<br>    <span class="hljs-keyword">return</span> left;<br>&#125;<br></code></pre></td></tr></table></figure><p>上面所提到的内容都属于链表中极为基础的部分,后续的与算法相关的内容我会新开一篇来讲</p><p>最后，本期镇楼图</p><p><img src="https://s1.imagehub.cc/images/2024/11/27/81fb0681c0cef5b8bb248a3b54556646.jpg" alt="efb29591 e59c 4325 a2fe cd60db1a6c19"></p>]]></content>
    
    
    
    <tags>
      
      <tag>C语言基础</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>浅谈Java中的文件处理</title>
    <link href="/2024/11/17/%E6%B5%85%E8%B0%88Java%E4%B8%AD%E7%9A%84%E6%96%87%E4%BB%B6%E5%A4%84%E7%90%86/"/>
    <url>/2024/11/17/%E6%B5%85%E8%B0%88Java%E4%B8%AD%E7%9A%84%E6%96%87%E4%BB%B6%E5%A4%84%E7%90%86/</url>
    
    <content type="html"><![CDATA[<h1 id="浅谈Java中的文件处理"><a href="#浅谈Java中的文件处理" class="headerlink" title="浅谈Java中的文件处理"></a>浅谈Java中的文件处理</h1><h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p><strong>本篇文章面向小白,各位大佬无需浪费时间</strong></p><p>今天来总结一下Java中的文件处理api，主要以<code>java.nio.file</code>包中的api为主（事实上，<code>java.io.file</code>中的相关api在过去更多的被使用，但nio包中的api技术更新，因此主要展示nio包中的api）</p><h2 id="Path-api"><a href="#Path-api" class="headerlink" title="Path api"></a>Path api</h2><p>path api 所对应的是文件的地址（可以是绝对的，也可以是相对的），从性质上来讲，它是一个引用。下面是一些简单的实例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Path</span> <span class="hljs-variable">path1</span> <span class="hljs-operator">=</span>Path.of(<span class="hljs-string">&quot;E:/some/s.txt&quot;</span>);<span class="hljs-comment">//注意，你无需区分/与\\,无论哪一种都可以被正常识别</span><br>System.out.println(path1);<br><span class="hljs-type">Path</span> <span class="hljs-variable">path2</span> <span class="hljs-operator">=</span>Path.of(<span class="hljs-string">&quot;E:/some/&quot;</span>).resolve(<span class="hljs-string">&quot;some.txt&quot;</span>);<br>System.out.println(path2);<br></code></pre></td></tr></table></figure><p>（如果你使用的是Java11或更以前的版本，应当使用<code>Path.get</code>而不是of）</p><p>我们可以先看看运行结果：</p><figure class="highlight ada"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs ada">E:\<span class="hljs-keyword">some</span>\s.txt<br>E:\<span class="hljs-keyword">some</span>\<span class="hljs-keyword">some</span>.txt<br></code></pre></td></tr></table></figure><p>这里其实很难给出一个恰当的对resolve的翻译，你可以简单的理解为在原来的Path上继续增加一段字符串,到后面会有进一步的解释</p><p>另外，<code>path.of</code>还支持另一种使用方法</p><p>像这样<code>Path path3 =Path.of(&quot;E:&quot;,&quot;/some&quot;,&quot;/some.txt&quot;);</code>但我个人对此并不推荐，显然它会使输入更加的麻烦</p><p><strong>应当注意的是path只是一个引用</strong>，他不能实际的确定文件是否存在，应当对此单独做出检查</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">System.out.println(Files.exists(path1));<span class="hljs-comment">//注意，是Files而不是File</span><br></code></pre></td></tr></table></figure><p>输出</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-literal">false</span><br></code></pre></td></tr></table></figure><h2 id="常见的文件操作"><a href="#常见的文件操作" class="headerlink" title="常见的文件操作"></a>常见的文件操作</h2><p>接下来我们将了解一些与文件的增删改查相关的操作</p><p>首先是创建目录与文件,这里我们需要使用Files包中相关的方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java">Path nweDir=Files.createDirectories(Path.of(<span class="hljs-string">&quot;E:/some&quot;</span>));<span class="hljs-comment">//创建目录</span><br>System.out.println(nweDir);<br>Path newFile=Files.createFile(nweDir.resolve(<span class="hljs-string">&quot;newFile.txt&quot;</span>));<span class="hljs-comment">//创建文件</span><br>System.out.println(newFile);<br></code></pre></td></tr></table></figure><p>在这里其实就可以看的出resolve的作用是什么</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">E:\some<br>E:\some\newFile.txt<br></code></pre></td></tr></table></figure><p>此时目录与文件就被顺利的创建了</p><p>另外,其实还存在着创建临时目录与文件的<code>Files.createTempDirectory()</code> <code>Files.createTempFile()</code>方法,但同样的,我并不推荐,应为它们虽然被称为临时文件,但并不会在某个时间点自动被删除,与其如此,不如直接使用一般的文件创建方式</p><p>接下来我们看一看文件的移动</p><p>就像这样</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Files.move(Path.of(<span class="hljs-string">&quot;E:/some/newFile.txt&quot;</span>), Path.of(<span class="hljs-string">&quot;E:/some/s/newFile.txt&quot;</span>));<span class="hljs-comment">//这一句的语法是正确的</span><br>Files.move(Path.of(<span class="hljs-string">&quot;E:/some/newFile.txt&quot;</span>), Path.of(<span class="hljs-string">&quot;E:/some/s&quot;</span>));<span class="hljs-comment">//这是错误的</span><br></code></pre></td></tr></table></figure><p>在移动对应的文件时应当指明其所对应的绝对路径,而不是指明将其移向哪个目录下</p><p>当然,如果文件已存在,该操作不会被执行,而是会抛出对应的异常,也可以这样</p><figure class="highlight coq"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs coq">Files.<span class="hljs-built_in">move</span>(<span class="hljs-keyword">Path</span>.of(<span class="hljs-string">&quot;E:/some/newFile.txt&quot;</span>), <span class="hljs-keyword">Path</span>.of(<span class="hljs-string">&quot;E:/some/s/newFile.txt&quot;</span>),StandardCopyOption.REPLACE_EXISTING);<br></code></pre></td></tr></table></figure><p>此时文件将会被强制覆盖</p><p>此外,我们应当了解如何获取当前目录下的文件列表</p><p>这分为两种情况,</p><p>1.仅获取当前目录下的文件,不对子目录的文件做要求</p><p>就像这样</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Path dir=Path.of(<span class="hljs-string">&quot;E:/some&quot;</span>);<br>Files.list(dir).forEach(System.out::println);<br></code></pre></td></tr></table></figure><p>值得一提的是list方法返回的是一个流,你可以对它进行一切流可以进行的操作,下面是输出结果</p><figure class="highlight dts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs dts"><span class="hljs-symbol">E:</span>\some\newFile.txt<br><span class="hljs-symbol">E:</span>\some\s<span class="hljs-comment">//这是一个目录</span><br></code></pre></td></tr></table></figure><p>2.获取内容包括子目录的文件</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Path dir=Path.of(<span class="hljs-string">&quot;E:/some&quot;</span>);<br>Files.walk(dir).forEach(System.out::println);<br></code></pre></td></tr></table></figure><p>walk方法会遍历所有的子目录,这是输出结果</p><figure class="highlight ada"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs ada">E:\<span class="hljs-keyword">some</span><br>E:\<span class="hljs-keyword">some</span>\newFile.txt<br>E:\<span class="hljs-keyword">some</span>\s<br>E:\<span class="hljs-keyword">some</span>\s\newFile2.txt<br></code></pre></td></tr></table></figure><p>但我们发现.有时候我们并不需要目录名,而只需要获取文件名,这时可以这样</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Files.newDirectoryStream(dir,<span class="hljs-string">&quot;*.txt&quot;</span>).forEach(System.out::println);<br></code></pre></td></tr></table></figure><p>这样我们就可以只获得对应目录下的TXT文件</p><p>然后是目录与文件的删除,这点略有麻烦,java并没有提供删除一个非空目录的方法,而是仅提供了删除空目录的<code>Files.delete</code>方法,类似于这样</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java">Path dir=Path.of(<span class="hljs-string">&quot;E:/some/s&quot;</span>);<br>       <span class="hljs-keyword">try</span>&#123;<br>           Files.delete(dir);<br>       &#125;<span class="hljs-keyword">catch</span> (DirectoryNotEmptyException e)&#123;<br>           System.out.println(<span class="hljs-string">&quot;Directory does not exist&quot;</span>);<br>       &#125;<br></code></pre></td></tr></table></figure><p>好吧我收回之前的话，再查了一通资料后我发现<code>java.io.file</code>包中就有直接的删除目录的方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">deleteDirectory</span><span class="hljs-params">(File directory)</span> &#123;<br>       <span class="hljs-keyword">if</span> (!directory.exists()) &#123;<br>           System.out.println(<span class="hljs-string">&quot;目录不存在: &quot;</span> + directory.getAbsolutePath());<br>           <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>       &#125;<br><br>       <span class="hljs-comment">// 如果是文件，直接删除</span><br>       <span class="hljs-keyword">if</span> (directory.isFile()) &#123;<br>           <span class="hljs-keyword">return</span> directory.delete();<br>       &#125;<br><br>       <span class="hljs-comment">// 如果是目录，递归删除子文件和子目录</span><br>       File[] files = directory.listFiles();<br>       <span class="hljs-keyword">if</span> (files != <span class="hljs-literal">null</span>) &#123;<br>           <span class="hljs-keyword">for</span> (File file : files) &#123;<br>               deleteDirectory(file);<br>           &#125;<br>       &#125;<br><br>       <span class="hljs-comment">// 删除目录本身</span><br>       <span class="hljs-keyword">return</span> directory.delete();<br>   &#125;<br><br>   <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>       <span class="hljs-type">String</span> <span class="hljs-variable">dirPath</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;path/to/directory&quot;</span>; <span class="hljs-comment">// 替换为你的目录路径</span><br>       <span class="hljs-type">File</span> <span class="hljs-variable">directory</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">File</span>(dirPath);<br><br>       <span class="hljs-type">boolean</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> deleteDirectory(directory);<br>       <span class="hljs-keyword">if</span> (result) &#123;<br>           System.out.println(<span class="hljs-string">&quot;目录删除成功: &quot;</span> + dirPath);<br>       &#125; <span class="hljs-keyword">else</span> &#123;<br>           System.out.println(<span class="hljs-string">&quot;目录删除失败: &quot;</span> + dirPath);<br>       &#125;<br>   &#125;<br></code></pre></td></tr></table></figure><p>这是我从网上找到的一段较为标准的删除目录树的方法,只使用了<code>java.io.file</code>包中的方法,相较于调用<code>java.nio.file.path</code>中的方法显得可读性更强,也可一看看使用path包的删除流程</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> (Stream&lt;Path&gt; walk = Files.walk(tmpDir)) &#123;<br>    walk.sorted(Comparator.reverseOrder()).forEach(path -&gt; &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            Files.delete(path);<br>        &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;<br>            <span class="hljs-comment">// something could not be deleted..</span><br>            e.printStackTrace();<br>        &#125;<br>    &#125;);<br>&#125;<br></code></pre></td></tr></table></figure><p>显然，还是<code>java.io.file</code>中的方法可读性强</p><p>在这一部分的最后,我们可以了解一下关于绝对路径与相对路径的关系,我个人推荐在使用过程中尽可能使用绝对路径如<code>E:/some/s</code>而不是</p><p><code>./s</code>(这代指当前目录下的s子目录)这样的相对路径,这总会带来奇奇怪怪的bug.所以涉及到的路径的转换方法略去不谈,有需要可以自行寻找</p><h2 id="文件的读与写"><a href="#文件的读与写" class="headerlink" title="文件的读与写"></a>文件的读与写</h2><p>文件的读写是程序很重要的一部分,但幸运的是,得益于前人的完善,这部分的工作相对比较简单</p><p>首先是读取文件</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java">Path path=Path.of(<span class="hljs-string">&quot;E:/some/newFile.txt&quot;</span>);<br>       String s= Files.readString(path);以字符串的形式读取<br>       String s2=Files.readString(path, StandardCharsets.UTF_8);<span class="hljs-comment">//以字符串的形式读取，并声明格式为UTF-8</span><br>       String s3= Arrays.toString(Files.readAllBytes(path));<span class="hljs-comment">//以字节的形式读取，声明格式为UTF-8</span><br></code></pre></td></tr></table></figure><p>默认将以utf-8的格式读取,你也可以通过对应的参数指定,就像s2与s3所展示的那样</p><p>当然,也可以选择使用流的方式进行读取,就像这样</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">BufferedReader</span> <span class="hljs-variable">bufferedReader</span> <span class="hljs-operator">=</span> Files.newBufferedReader(utfFile)<br><span class="hljs-type">InputStream</span> <span class="hljs-variable">is</span> <span class="hljs-operator">=</span> Files.newInputStream(utfFile)<br></code></pre></td></tr></table></figure><p>接下来是文件的写入,同样的非常简单</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Files.write(path,<span class="hljs-string">&quot;this is a String&quot;</span>.getBytes(StandardCharsets.UTF_8),StandardOpenOption.APPEND);<br>Files.writeString(path, <span class="hljs-string">&quot;this is my string ää öö üü&quot;</span>, StandardCharsets.ISO_8859_1);<br></code></pre></td></tr></table></figure><p>写和上面的读一一对应,默认的格式为UTF-8,但你也可以自己指定</p><p>不过需要强调的是,</p><ol><li><p>如果对应的path不存在,文件将会被自行创建</p></li><li><p>在默认模式下,原文件的内容将会被覆盖,可以添加参数<code>StandardOpenOption.APPEND</code>使添加的内容会在原内容尾被添加,下面是一些可用的(但不是全部)StandardOpenOption,可以了解一下</p><table><thead><tr><th><strong><code>APPEND</code></strong></th><th>将数据写入文件末尾（追加模式）。如果文件不存在，会抛出异常，除非同时指定了 <code>CREATE</code>。</th></tr></thead><tbody><tr><td></td><td></td></tr></tbody></table><table><thead><tr><th><strong><code>CREATE</code></strong></th><th align="left">如果文件不存在，则创建文件。如果文件已存在，则不会影响文件的内容。</th></tr></thead><tbody><tr><td></td><td align="left"></td></tr></tbody></table><table><thead><tr><th><strong><code>CREATE_NEW</code></strong></th><th>如果文件不存在，则创建新文件。如果文件已存在，则抛出 <code>FileAlreadyExistsException</code>。</th><th></th></tr></thead><tbody><tr><td></td><td></td><td></td></tr></tbody></table><table><thead><tr><th><strong><code>DELETE_ON_CLOSE</code></strong></th><th>在关闭文件时删除文件。这通常用于临时文件操作。</th></tr></thead><tbody><tr><td></td><td></td></tr></tbody></table></li></ol><p>同时,流的使用以没有任何问题</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">BufferedWriter</span> <span class="hljs-variable">bufferedWriter</span> <span class="hljs-operator">=</span> Files.newBufferedWriter(utfFile)<br><span class="hljs-type">OutputStream</span> <span class="hljs-variable">os</span> <span class="hljs-operator">=</span> Files.newOutputStream(utfFile)<br></code></pre></td></tr></table></figure><p>最后,推荐在有能力的情况下亲自取阅读官方给出的文档,并了解:</p><ol><li>内存文件系统</li><li>文件变化监测</li></ol><h2 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h2><p>终于写完了!!!</p><p>当然,虽然花费了不少的心血,但受限于个人的学识与能力,难免会有各种疏漏之处,还望见谅,如有疑问或发现错误,欢迎通过邮箱联系</p><p>老样子,放张图镇楼</p><p><img src="https://s1.imagehub.cc/images/2024/11/17/69141a3a8a16e46ddbfffaa8fd4602b6.jpg"></p>]]></content>
    
    
    
    <tags>
      
      <tag>Java基础</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>小白的第一篇blog</title>
    <link href="/2024/11/11/firstblog/"/>
    <url>/2024/11/11/firstblog/</url>
    
    <content type="html"><![CDATA[<h1 id="小白的第一篇blog"><a href="#小白的第一篇blog" class="headerlink" title="小白的第一篇blog"></a>小白的第一篇blog</h1><p>出于好玩，花了一天的时间创建了这个基于hexo的blog，十分的朴实无华，但至少，它能用了。</p><p>以后呢会在这里放一些自己的技术实践和总结文章，最近主要会以Java为主，也可能会有部分C语言</p><p>当然，小白一个，如有不足还望指正</p><p>最后呢，放一张图镇楼：</p><p><img src="https://s1.imagehub.cc/images/2024/11/10/a26ab25b5682981a5f86b4b93ada1383.jpeg" alt="0ab23a20d16a3087d99730e05f67fb44"></p>]]></content>
    
    
    
    <tags>
      
      <tag>杂谈</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
